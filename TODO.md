# Improvements
- add a queue for operations (e.g. deleting) to prevent API limits
- improve .env loading
- add logging
- metadata at top

# ValFile Operations
- rename
- move (out of folder)

# ValFS Operations
- deno.json automatic caching
